import React from 'react';
import { Outlet } from 'react-router-dom';

import './<%= block.fileName %>.scss';

class <%= block.className %> extends React.Component {
  render() {
    return (
      <div className="<%= block.className %>">
        <Outlet />
      </div>
    );
  }
}

export default <%= block.className %>;
